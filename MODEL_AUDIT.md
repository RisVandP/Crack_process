# 模型审计记录

本项目实现 `cracked_board_processing_model_robust_rolling.tex` 中的确定性基础模型。
当前阶段只实现确定性 0-1 整数线性规划模型（BILP），不实现滚动重调度和随机情景扩展。

## 已采用的建模口径

1. 木板按宽度方向 `m` 份、长度方向 `n` 份等分，自动生成 `m*n` 个木块。
2. 相邻边集合 `E` 只包含上下左右公共边，不包含斜对角。
3. 示例采用全局裂缝几何输入：输入裂缝折线 `polyline` 与宽度 `width`，由程序计算 `C_u`、`CS_u` 和 `L_uv`。同时保留直接输入模式，便于调试和兼容附件中“至少实现直接输入模式”的要求。
4. 木块固有价值按 LaTeX 当前定义实现：`v_u = base_value * (1 - CS_u)`。
5. 设备时间约束采用共同工期 `T`，即每台设备的总加工时间不超过同一个截止时间。
6. `h_uv` 不使用变量乘积，使用三条线性约束刻画双精加工状态。
7. 目标函数保留与决策无关的常数项 `sum(v_u)` 和 `sum(L_uv)`，并在输出中单独报告。

## 与题目或文档可能存在的差异

1. 当前几何预处理只支持折线裂缝与规则矩形网格的解析计算，不涉及图像识别；如果原题提供图像形式裂缝，需要先转换为折线坐标。
2. LaTeX 全文后续包含设备故障、性能下降、隐藏裂缝和滚动重调度内容，本阶段明确不实现，代码仅预留清晰模块边界。
3. 当前 LaTeX 摘要中仍有“混合整数线性规划模型”表述；本阶段代码和 README 统一称为“确定性0-1整数线性规划模型（BILP）”，因为本阶段决策变量全为0-1变量。

## 建议

后续若需要扩展更复杂裂缝形态，可继续扩展 `crack_geometry.py`，但不应改变确定性 BILP 的求解接口。
