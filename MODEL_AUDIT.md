# 模型审计记录

## 求解方向调整

项目已由“调用包求解器求解 0-1 线性模型”调整为“保留数学目标与约束，自行实现确定性启发式算法”。

已删除运行层面对 CBC、Gurobi、PuLP、OR-Tools 等包求解器的依赖和调用。`requirements.txt` 中不再包含整数规划或线性规划求解器依赖。

## 保留的数学含义

1. 木块按规则网格等分，自动生成木块集合和上下左右相邻边。
2. 加工时间仍为 `A/s_k*(1+alpha*C_u*CS_u)`。
3. 普通加工只能分配普通设备，精密加工只能分配精密设备。
4. 每台设备累计加工时间不能超过共同工期。
5. 完整目标函数仍包含固有价值、加工增值、相邻双精加工奖励、精密状态不一致损失和跨块裂纹损失。

## 常数项审计

`v_u = base_value*(1-CS_u)` 表示裂纹折损后的木块固有价值。在当前模型中，无论木块是否加工，`v_u` 都计入最终目标函数，因此 `sum(v_u)` 是决策无关常数。

`L_uv` 在给定裂纹输入后也是决策无关常数，但仍影响最终成品价值，所以保留在目标值、目标分解和实验比较中。

影响决策的目标项主要是：

- 普通加工增值；
- 精密加工增值；
- 相邻双精加工奖励；
- 精密状态不一致损失。

## 排序分数与最终目标

`ValueFirst` 和 `ValueDensityFirst` 的排序分数只用于构造简单基线：

- `ValueFirst`: `v_u + A*r_q`
- `ValueDensityFirst`: `(v_u + A*r_q)/hat_t[u,k]`

这两个排序分数不等于最终目标函数。所有算法生成方案后，都统一重算完整目标函数。

## 自研算法边界

- `MarginalGreedy` 每轮使用完整目标函数计算真实边际收益。
- `CNAG-LS` 复用动态边际收益构造，并加入相邻成对精加工候选和多邻域局部搜索。
- 小规模精确回溯由 Python DFS 自行枚举，仅用于验证，不作为主算法。
- 项目不实现或声称实现 CBC、Gurobi 等求解器的底层算法。

## 题意差异与简化

题目提到带裂纹木块加工更慢。代码使用裂纹严重程度修正时间，而不是为每台设备输入“无裂纹速度/有裂纹速度”两套参数。这与 LaTeX 模型当前公式一致，是对题意的参数化简化。

当前裂纹几何预处理支持折线裂纹与规则矩形网格，不涉及图像识别。
